[DEFAULT]
OPENAI_API_KEY = [OpenAI API Key]
GROUP_URL = https://www.facebook.com/groups/[groupName]
WEBHOOK_URL = [discord webhook URL]
FACEBOOK_XS_COOKIE = [insert facebook XS cookie content, this cookie changes everytime you disconnect & reconnecte to your acc]
FACEBOOK_CUSER_COOKIE = [c_user cookie, this one remains the same, it's your accound identifier ID]
